#include <stdio.h>
#include <ctype.h> 
#define MAX_LENGTH 4 
int main() {
    int t;
    char s[MAX_LENGTH];

   
    scanf("%d", &t);


    for (int i = 0; i < t; i++) {
        
        scanf("%s", s);

        
        for (int j = 0; j < 3; j++) {
            s[j] = tolower(s[j]);
        }

       
        if (strcmp(s, "yes") == 0) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }
    }

    return 0;
}
